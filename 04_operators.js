// operators are special symbols or keywords used to perform operations on values or variable

/// arithmetic operators + - * / ++ -- % **

let age = 90;
age--;
console.log(age);

console.log(20 ** 3);
/// % modulus --gets us the remainder in whole number oprations

// assignment operator
age = 20;
// aritmentic operator and assignment e.g. *= , -= , +=
age /= 5;

console.log(age);

// comparison operators   > < == >= <=

console.log(20 >= 20);

// logical operators
// !  NOT
console.log(!true);
console.log(20 !== 20);

// && AND
console.log(20 > 10 && 5 < 10); // true && true == true

// || OR
console.log(20 < 10 || 5 < 10); // false || true == true

// THE TRUTH TABLE
// es6 operators --- optional chaining operator, nullish coalesing operator, spread operator, rest operator,  ternary operator
