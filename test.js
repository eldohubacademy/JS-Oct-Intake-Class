let user = {
  name: "albert",
  age: 50,
  married: true,
};

// Object

console.log(Object.keys(user)); // ["name", "age", "married"].includes()
console.log(user["name"]);
// console.log( user."name" );
